# Minimal requirements for Vercel deployment
# Note: Large dependencies like torch, osmnx may cause build timeouts
# Consider using lighter alternatives or pre-built Docker images

streamlit>=1.36.0
pandas>=2.2.0
numpy>=1.26.0
folium>=0.16.0
python-dotenv>=1.0.1
requests>=2.32.0

# Core dependencies (may need to be pre-installed or use Docker)
torch>=2.3.0
torch-geometric>=2.5.0
osmnx>=1.9.0
networkx>=3.2.0
shapely>=2.0.0
geopandas>=1.0.0

